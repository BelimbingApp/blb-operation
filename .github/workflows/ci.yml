name: ci

# Thin caller for the BLB Operation Domain. All build/test/scan logic
# lives once in the platform repo's reusable domain-ci workflow; this repo only
# declares its contract path and SonarCloud identity.

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  ci:
    uses: BelimbingApp/belimbing/.github/workflows/domain-ci.yml@main
    with:
      domain-path: app/Domains/Operation
      sonar-project-key: BelimbingApp_blb-operation
      sonar-organization: belimbingapp
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
